server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    location / {
        # SPA fallback: the app does its own client-side routing (e.g. /doc/<id>),
        # so any path that isn't a real static file should still serve index.html.
        try_files $uri /index.html;
    }
}
